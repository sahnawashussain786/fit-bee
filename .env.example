# Environment Variables for Vercel Deployment

# MongoDB Connection
MONGODB_URI=your_mongodb_connection_string_here

# Gmail Configuration for Nodemailer
EMAIL_USER=your_gmail_address@gmail.com
EMAIL_PASS=your_gmail_app_password_here

# JWT Secret for Authentication
JWT_SECRET=your_random_secret_key_here

# Note: 
# 1. For EMAIL_PASS, use a Gmail App Password, not your regular password
# 2. Generate an App Password at: https://myaccount.google.com/apppasswords
# 3. For JWT_SECRET, use a long random string (e.g., generate with: openssl rand -base64 32)
# 4. Add these variables in Vercel Dashboard: Settings → Environment Variables
