{
  "name": "fitlife-api",
  "version": "1.0.0",
  "type": "module",
  "description": "Serverless API functions for FitLife",
  "scripts": {
    "build": "cd client && npm install --include=dev && npm run build && mkdir -p ../public && cp -r dist/* ../public"
  },
  "dependencies": {
    "mongoose": "^8.0.0",
    "nodemailer": "^6.9.7",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2"
  }
}
